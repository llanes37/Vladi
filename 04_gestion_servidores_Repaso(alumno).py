# -------------------------------------------------------------------------------------------
# SISTEMA DE GESTIÓN DE SERVIDORES - VERSIÓN PARA EL ALUMNO
# ? Este archivo está diseñado para que el alumno implemente las funciones necesarias para
# ? gestionar servidores. Sigue las indicaciones de cada sección y completa el código.
# -------------------------------------------------------------------------------------------

# * SECCIÓN 1: DEFINICIÓN DE VARIABLES Y LISTAS
# TODO: Define una lista de servidores con sus estados iniciales.
# servidores = [
#     {"nombre": "Servidor_1", "cpu": 90, "memoria": 2048, "activo": True},
#     {"nombre": "Servidor_2", "cpu": 50, "memoria": 4096, "activo": True},
#     {"nombre": "Servidor_3", "cpu": 95, "memoria": 1024, "activo": False},
# ]
servidores = []  # Completa esta parte.

# -------------------------------------------------------------------------------------------
# * SECCIÓN 2: FUNCIONES

# TODO: Define una función para mostrar el estado de todos los servidores.
# ? Debe iterar sobre la lista e imprimir el estado de cada servidor.
# def mostrar_estado_servidores(servidores):
#     pass

# TODO: Define una función para optimizar un servidor.
# ? Si está activo, reduce la carga de CPU y aumenta la memoria. Si no, muestra un error.
# def optimizar_servidor(servidor):
#     pass

# TODO: Define una función para verificar el estado de los servidores.
# ? Debe mostrar alertas si la carga de CPU es alta y sugerir reiniciar si está inactivo.
# def verificar_servidores(servidores):
#     pass

# TODO: Define una función para reiniciar un servidor.
# ? Cambia el estado a activo y reinicia sus valores de CPU y memoria.
# def reiniciar_servidor(servidor):
#     pass

# -------------------------------------------------------------------------------------------
# * SECCIÓN 3: INTERACCIÓN DEL USUARIO

# TODO: Define una función principal para gestionar los servidores.
# ? El menú debe incluir opciones para mostrar, verificar, reiniciar y salir.
# def sistema_gestion():
#     pass

# TODO: Llama a la función principal para iniciar el sistema.
# sistema_gestion()

# -------------------------------------------------------------------------------------------
# * NOTAS PARA EL ALUMNO:
# - Usa condicionales y bucles para manejar la lógica del sistema.
# - Prueba cada función por separado antes de integrarla en el sistema.
# -------------------------------------------------------------------------------------------
